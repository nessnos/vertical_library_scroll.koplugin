local _ = require("gettext")
return {
    name = "vertical_library_scroll",
    fullname = _("Vertical scrolling library"),
    description = _([[Browse the file browser / library as vertically scrolling pages (swipe up/down, with a Kindle-style scrollbar and up/down arrows on the right edge) instead of the stock horizontal swipe/arrows. Off by default; toggle it in File browser -> Settings.]]),
}
